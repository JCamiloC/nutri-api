PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3002

# Postgres local (Docker Compose o instalación nativa)
DATABASE_URL=postgresql://nutri:nutri@127.0.0.1:5432/nutri_lab

# Opcional: Neon / RDS / otro Postgres en la nube (misma variable)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# USDA FoodData Central — solo servidor (nunca en el front)
USDA_API_KEY=
